import React from "react"

const Topo = () => (
    
    <nav className="navbar navbar-dark bg-dark">
        <span className="navbar-brand mb-0 h1">greenmile</span>
    </nav>

);

export default Topo;